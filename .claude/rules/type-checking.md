# Type Checking

After completing code changes, run the appropriate type check command:

- **Frontend files** (`.svelte`, `.ts` in `frontend/`): `just check-frontend`
- **Backend files** (`.py`): `just check-backend`
- **Both**: `just check`

Always ensure type checks pass before considering a task complete.
